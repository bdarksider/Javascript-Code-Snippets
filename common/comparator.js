function notificationComparator(compareTo, compareFrom) {
    for (var i = 0; i < compareFrom.length; i++) {
        for (var j = 0; j < compareTo.length; j++) {
            if (/* checks */) {
                // actions
            }
        }
    }
}
